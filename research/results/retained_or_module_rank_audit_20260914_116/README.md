# Complete retained OR witness audit

The [notebook entry](https://kbr.is-a.dev/math-research/#entry-2026-09-14-retained-or-witness-rank)
contains the full source construction, scalar restriction, degree ledger,
actual child normalization, and scope of the matrix-width obstruction.
This cycle is an analytic composition, with no new numerical experiment.

## Exact dependencies

- The notebook's proper-OR-union-complete-support section supplies the full
  ordinary NS identity and its collected coefficient -P2*U0_i.
- selector-valued-NS-gate-modules supplies the original three-value NS ceiling.
- balanced-code-half-coordinate-rank and affine-flat-evaluation-cut-rank
  supply the every-order matrix lower bound.
- all-order-hard-selector-degree-normalization supplies the actual affine
  child normalization and original-degree companion witnesses.
- row-linear-bit-quotient-injection and compact-bit-transported-filtration
  supply the scoped old-quotient nonvanishing statement.

For h>=2, the proper comparison uses 2h+1 independent old signals on one side
and h nonlinear values 1-V_b+z on the other. The V_b are genuine lower products
with the complete code matrix construction from cycle 115. All coefficient
scopes are fresh and disjoint. The full source NS ceiling is 4h^2+4h.

The representation restriction selects only the first union row's first old
signal, and uses identity coefficient matrices below and on the nonlinear
side. Its surviving collected cofactor is the code product; choosing one
companion placeholder extracts it from the complete supplied certificate.
This restriction is not itself asserted to preserve a refutation.

The separate source normalization leaves union/affine-side coefficients and
the modifier free, removing only the lower-plus-nonlinear-child package using
the previously checked affine map and its complete NS templates.

The original code-and-cut-certificates.jsonl from cycle 115 is referenced and
hashed by provenance, without duplicating its contents or rerunning its tests.
Timing and complete command evidence are archived with this checkpoint.
