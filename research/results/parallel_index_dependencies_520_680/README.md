# Direct dependency review: positions 520–679

The complete assigned range is now covered: **159 reviewed inventories**, with
previously reviewed publication theorem position553 preserved. No canonical
registry/notebook/Git edits were made by this worker. This is source-relative
metadata review, not fresh proof verification or a literature/novelty audit.

`delivery.json` lists the complete patch order and counts. Apply
`patch_resolutions.json` **last**: it supersedes three provisional pending
inventories after exact provenance checks, retaining previously recorded edges.
The coordinator should reconcile existing edge identities and refresh endpoint
review hashes against the final combined registry. Structural union validation
passed; the canonical source texts and formalization dispositions remain intact.

Source passages were inspected in bounded thematic clusters. Explicit proof
inputs, applications, comparisons, qualified refinements and historical source
formalizations are separated. Shared anchors do not automatically imply shared
dependencies. In particular generic relabeling does not depend on its chessboard
application, and ordinary dimension bounds do not depend on row-linear examples.
Imports alone are not theorem dependencies; basic library algebra is described
in review notes rather than expanded into a second library graph.

Two source anchors contained only the statement before unanchored sibling proof
headings. For relabeling and the homological cover lemma, the containing article
was inspected and used as review evidence; Boolean reduction and matching normal
form likewise required their unanchored proof/row-equation subsections. This
retrieval limitation was reported to the coordinator.

Three resolved provenance questions:

- Position532 reuses explicit finite residual degree-two path data from the
  earlier binary and signed-F3 finite checks. Its checker retains the exact
  three-cell path and signs; it does not need Razborov existence for that fixture.
- Position555 reuses the all-prime affine proof's literal coordinate-ideal
  argument, whose field-independent monomial proof is in the ordinary-restriction
  audit. The changed multiplier dimension bound is local to555.
- Position610 uses that binary literal representation while retaining its actual
  k+1 original-generator NS cost, rather than silently replacing it by k.

`build_patch*.py` reproduce proposed metadata; `read_sources.py`, `read_prose.py`
and `links.py` record the retrieval approach. Complete protected command outputs
are archived under sessions `parallel_index_dependencies_520_680` and
`parallel_index_dependencies_551_680`. Their timing overlaps other workers and
must not be summed as serial wall time. No dependencies were installed and no
historical numerical or Lean suite was replayed.
