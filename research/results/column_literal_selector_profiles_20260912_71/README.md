# Column-literal selectors and a route reassessment

The [notebook entry](https://kbr-.github.io/math-research/#entry-2026-09-12-column-literal-selector-profiles)
contains the exact common-zero classification, all coefficient choices and NS
identities, the arbitrary-partition realization, and the user's requested candid
assessment of progress toward the lower bound.

This is a local source-syntax result. It does not establish the central
source-family elimination obligation. Positive literal OR tuples can realize
arbitrary joint occupied partitions with logarithmically many selectors, despite
one level, polynomial inventory size, and sharp Booleanity. The construction
does not assert that those blocks are essential to a short PHP proof.

The degree checks are explicit algebra: constants or opposing inputs give a zero
selector; two negative literals give a column collision; one negative literal
gives its cell indicator; positive literals give the complementary column sum.
Repeated input coordinates are retained, with only selected representatives
receiving nonzero coefficients. All original companion and field budgets fit.
No numerical run or historical suite rerun was required.

Dependencies include the
[selector-only source audit](https://kbr-.github.io/math-research/#source-selector-only-dependency),
[conditional hybrid transfer](https://kbr-.github.io/math-research/#selector-only-hybrid-transfer),
and [existing collision-clause map](https://kbr-.github.io/math-research/#virtual-php-affine-map).

The user questioned whether the accumulated lemmas were advancing the goal.
The recorded assessment is that recent work mostly explored conditional tools
without closing the central coverage gap. The next cycle is a route audit;
the proposed additional sparse-family example is deferred. PHP remains the target.

The session began with the separately committed reference-checker improvement,
including an actual version check for the new source and a synthetic mismatch
control that changed no source file. A small claim-index table repair in the
preceding unpublished checkpoint was also made during this initial window.
Mathematics includes the classification, its scope analysis, and the route
discussion. The timing fragment, archived session, and provenance preserve this
measurement scope; framework checks are not presented as mathematical experiments.
