# Parallel source-relative dependency inventory: positions 48–159

This worker reviewed all 112 claims in the coordinator-assigned half-open range
[48,160), using the equation-inclusive dependency candidates and targeted local
statement/proof/application sections. The complete notebook was not loaded.
The claim source ranges are preserved by IDs and core/source SHA-256 values in
the proposal files; ordinal positions only identify this pinned assignment.

Integration order:

1. `patch.json`: positions 48–87, 40 reviewed inventories, 83 edge proposals.
2. `patch_continued.json`: positions 88–159, 72 inventories, 195 edge proposals.
3. `patch_review_amendment.json`: remove one misdirected correction proposal and
   add the two correct repair targets, for 279 proposed edges total.

The amendment is material: the strict-support lemma repairs the original
negative-leaf and all-axiom-root automatic applications. It does not retract the
valid audit identifying same-level proper OR subgroups. Delivered proposal files
are immutable; the amendment records this review correction explicitly.

The coordinator owns canonical registry, notebook, plan and Git edits. Preserve
already reviewed edge scopes when deduplicating the six equivalent existing
scope variants listed in `validation.json`; none is a mathematical conflict.
Normalize `BIKPRS-1996-simulation` to the coordinator's date-free
`BIKPRS-audited-source-inputs` endpoint: the cited audit is the provenance, and the
worker did not independently establish the publication year. Preserve the exact
per-edge source-result descriptions and source-audit locator.

The 127 candidate decisions record actual reviewed roles. Broad/shared-region
candidates that do not represent one unique selected role are rejected in favor
of their explicit scoped edges. The coordinator should apply decisions against
the same fingerprinted candidate inventory and refresh every affected incident
endpoint's relationship review digest after merging all workers.

## Scope and verification

This is metadata inventory, not new mathematics or proof re-verification. It
reuses recorded finite certificates and source audits, and performs no Lean
build, old numerical replay, external paper reread or new literature audit.
Standalone elementary constructions retain that disposition rather than gaining
invented prerequisites. Application-only dependencies and historical contrasts
are separated from premises of the abstract claims. In particular, a supplied
PC witness is not represented as an NS witness, and source-schedule corollaries
retain their copied-input, freshness, current-system and protected-goal premises.

`validate_patch.py` validates the additive union in memory without writing the
canonical registry. All 306 proposed source/locator references resolve locally,
all 112 claim/evidence fingerprints are current, and no dependency cycle is
introduced. The existing citation-only MOD interpolation cycle is separately
reported. Six repeated edge IDs have equivalent scoped descriptions and should
be retained once.

The worker's initial sandboxed status probe could not access user systemd; the
escalated status check confirmed the shared watchdog and resource boundary
before protected jobs. One patch construction failed because a nonheading
source anchor cannot be fingerprinted as an exact notebook heading; it was
resolved to the already reviewed containing section. No mathematical output was
lost and no computation bypassed resource controls.

## Reproduction

`read_sources.py START STOP` reads exact indexed source passages for a selected
range (with disclosed containing-section fallback for nonheading anchors).
`build_patch.py` and `build_patch_continued.py` build additive proposal JSON only.
`validate_patch.py` writes the review amendment and validates the proposals.
All substantive command runs used the shared protected launcher and the unique
session `parallel_index_dependencies_48_160`. Complete source extraction output
and timings are archived by that session. Do not rerun a builder over a delivered
proposal path; use a new path for a superseding proposal.
