# Modular source audit and adaptive conflict probability

The [notebook entry](https://kbr-.github.io/math-research/#entry-2026-09-12-modular-source-and-probability-audit)
contains the complete mathematical record. The targeted literature search
identified relevant parity-resolution techniques but no direct transfer to the
project's ENS endpoint. Krajicek's pseudo-solution argument gives a source-backed
alternative for the separately recorded direct NS endpoint.

The rate form extracted and proved in the entry permits a finite weighted
distribution of ordinary designs. For S companions and accuracy h it supplies
a conflict tree of height h+ceil(log2 S), with non-conflict probability at most
S*(1-1/p)^h. The proof retains original active-degree budgets and the fallback
to an input factor when no queried linear factor has value zero.

Under explicit binary query-height accounting, the printed diagonal guarantee
e=r*log2(n), gamma=n^(-r) cannot directly meet this sufficient inequality.
This is an application-parameter gap, not a refutation of the source theorem.
No qualifying distribution is asserted.

The new F2 covariance probe gives a deterministic tree of height t+1 with
non-conflict probability at most k+(1-k)*2^(-t), where
k=E[2^(-rank B_omega)]. Its full proof is an exact two-case count.
A residual lift preserves this covariance rank. The next task tests the actual
residual distribution and its higher-degree extension constraints.

## Sources and exact scope

sources.json records the search queries, primary URLs, pinned versions, and
reading scope without copying full-text search results into public evidence.
Web-only papers were inspected at the indicated statements/abstracts; their
full proofs were not independently checked. No new source PDF was saved locally.

For the already available Krajicek v3 source, the relevant extracted passages
were Definition 2.1; Definition 3.1, Lemmas 3.3-3.4, Theorem 3.2 and its proof,
Problem 3.5; Definition 4.2, Lemma 4.3, Problem 4.4; and Theorem 5.2.
The original typeset pages 7, 9, and 12 were rendered and inspected to confirm
the probability/height parameters and the PHP question. This was read-only
source verification, not notebook rendering or PDF authoring. The public
CC BY source PDF and extraction are fingerprinted in provenance.json.

The direct-PHP NS endpoint and existing claim indexes were also consulted.
No earlier covariance or pseudo-solution claim was found in those indexes.

## Measurement and limits

The initial preparation interval finishes the preceding research checkpoint.
Network-marked intervals include query preparation and source triage; reading
and mathematics were marked for the subsequent detailed analysis. Mathematics
also includes exploratory analysis of the proposed residual-moment test.
The three source-page renders ran concurrently under the same shared resource
limits and are counted once in the timing report. All three succeeded.

No numerical experiment, broad regression suite, or dependency installation was
performed. The covariance-rank distribution is an open obligation, not inferred
from the proof of the probe formula. The timing export and archived commands
preserve the actual measured scope.

The existing source-hypothesis guidance was sufficient to catch the model,
depth, and probability-rate mismatches. No additional framework rule was needed.
