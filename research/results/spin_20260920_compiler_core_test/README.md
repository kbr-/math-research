# Coherent spreads and shared-bit sum normalization

The [notebook entry](https://kbr.is-a.dev/math-research/#entry-2026-09-20-coherent-spread-and-shared-bit-sums)
realizes the historical spread in actual upper whole-value input spaces and
computes the exact bound of the specified raw nested-core criterion. It also
gives a complete affine substitution handling the same two-level family without
any NS or PC degree loss, over every prime for the stated Boolean shared-bit
setup. The expensive raw criterion does not establish proof or source hardness.

The construction is symbolic; there was no new finite-field enumeration. It
makes no claim that its gates occur essentially in a short PHP refutation or
that arbitrary later blocks are covered. The new all-prime shared-bit sum map
extends the recorded direct-product-input map; its binary exclusion scope was
already covered by the broader rank-two-bottom theorem.

This cycle also includes measured work on the separate framework checkpoint
4f4fa32, which surfaces historical statements in ordinary claim search. All 24
focused registry tests passed. The subsequent search immediately retrieved the
historical spread, so the current result is explicitly its application rather
than a new spread construction. The metadata check is not a mathematical proof
checker. No additional rendering suite was run.
