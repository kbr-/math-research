# R14 cube operator prerequisite

Continue R14 from committed packing prerequisite c273d01. Define a binary cube of substitutions indexed by epsilon:I→F2, where each input variable is either tagged by one selected index i and maps to a constant depending only on epsilon(i), or is untagged and maps to one fixed polynomial of degree≤1. Sum the restriction homomorphisms linearly.

Required helper: this sum lowers ordinary total degree by |I| (natural subtraction); a monomial missing a selected tag cancels by the fixed-point-free bit-flip involution, and a monomial containing every tag loses at least|I| degree when tagged variables become constants. If every source generator restricts to zero or one fixed residual generator independently of epsilon, the operator sends each legal NS_B generator multiple into NS_(B−|I|), then transports a residual annihilator. No unknown separator or degree-drop property is assumed in place of its proof.

This is a project-formulated reusable finite-difference interface; no novelty assertion. R14 board-specific restriction/axiom images, actual R13 coefficient isolation and final PC exclusions remain separate obligations. Full human-readable alternative proof will be recorded. Parent owns living sections/route. Do not start optional generalization sweep before allRxx are done.
