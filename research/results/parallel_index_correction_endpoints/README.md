# Correction endpoints and inventory-role closure

`status-review-proposals.json` reviews the 35 actual correction/supersession
edges in the final proposal, covering 27 distinct targets. Twelve targets have
source-backed **retain-current-status** reviews. Fifteen NA-related targets are
explicitly reserved to the coordinator; no NA mathematical status is changed.
The proposals pin the live input registry hash, core fingerprints and source
hashes. Preserve later coordinator edits and check those pins before applying.

The earlier merger report listed 36 obligations because it accumulated one
proposal before a later amendment removed it. That report-only bookkeeping bug
is fixed: obligations are filtered against the final edge set. The valid
`obs:proper-or-subgroups-share-level` obstruction is not retracted or corrected
by the strict-support repair, and has no resulting status-change obligation.

Review distinctions:

- Nested projection syntax repair leaves finite polynomial identities intact.
- Strict-support certificates repair the negative-leaf/all-axiom applications
  with revised ceilings, rather than restoring their original faulty reasoning.
- Positive finite interface checks survive the later counterexample to their
  universal extrapolation.
- Cube-volume threshold rebalancing partially corrects a frontier assessment.
- The adaptive query rule is refuted; new hole-only/both-endpoints algorithms
  do not make the original algorithm true.
- Rank constants and tree conventions are local corrections. Probability-source
  gaps require their separate NA audit and are not resolved by those repairs.
- Decoder composition with a supplied probability bound remains a conditional
  implication; applications cannot inherit an invalid estimate as if it were
  proved. Supersession removes stated hypotheses or changes the tree, not all
  limitations of the predecessor.

`inventory-resolutions.json` closes 29 *relationship-role* dispositions from
this worker's earlier batch. The false imported premise and direct/inherited
uses are now explicitly identified; that is complete provenance despite an
unrepaired mathematical argument. The two historical repair-ownership questions
(window structure and height budget) were resolved against exact original and
later source sections. No mathematical-status field is included in this patch.

## Final merger manifest

Use `../parallel_index_dependency_merge/manifest-final.json`. It adds the
37 duplicate-review relationships: 25 matched existing endpoint/type edges and
12 were new. The duplicate reviewer independently compared all 25 alternative
scope descriptions; existing sharper scopes are preserved and both wordings
remain in the merger report. It also applies the 29 inventory resolutions last.

The coordinator explicitly authorized resolving its 20 pending audit/source
inventory questions. Exact fingerprints in `resolve_pending_reviews` allow only
those particular questions to be closed by the source inventories; their audit
edges/evidence and mathematical statuses are preserved. A different or later
pending question remains protected. Two new tests cover these cases; all nine
merger tests pass.

Latest dry-run artifacts: `../parallel_index_dependency_merge/proposal-final.json`
and `report-final.json`. The run had zero conflicts or dependency cycles, with
all source/target checks passing. Rerun against final live edits. No canonical,
notebook or Git changes were made by this worker.
