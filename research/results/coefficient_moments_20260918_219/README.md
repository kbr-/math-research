# Cycle 219: feasibility of second-level tests on a PHP base; literature positioning

Session `coefficient_moments_20260918_219` (named for the computation that was planned and
then dropped). Supports the notebook entry `entry-2026-09-18-level-two-feasibility`.

- `level_two_feasibility.txt`: the number of partial matchings of size at most `d` on the
  board with `N+1` rows and `N` holes, for every `d` below the refutation degree `N/2 + 1`,
  with the degree at which pure second-level companions (degree five) enter and the degree of the
  recorded rank-three loss (seven). The file does not list the form companion of a mixed block
  (degree four), which the entry adds after review: it enters one row earlier. Produced by the inline script recorded in the session log.

No other computation was run. The two papers read for the entry are cited in
`research/references/references.bib` (IMP2023, DMM2023), with reading locators and hashes in
`research/notes/SOURCE_AUDIT.md`; their PDFs are kept in the private, Git-ignored directory.
