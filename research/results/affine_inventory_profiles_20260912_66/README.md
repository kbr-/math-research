# Affine inventory profiles and a freezing control

The [notebook entry](https://kbr-.github.io/math-research/#entry-2026-09-12-affine-inventory-profiles)
contains the exact recognition proof, the affine degree-two freezing obstruction,
and the full scope control. This is an analytic cycle with no new numerical run.

Equal coefficient signatures in each column give the coarsest local partition
for fixed affine inputs, literally or modulo the proper domain/column ideal.
The minimal total exception count is the sum of n+1 minus each column's largest
signature multiplicity. A basis change in the same input span preserves it.
Changing inputs by row equations is outside this fixed-inventory assertion.

Separating affine column probes also exclude more general affine freezing maps
with base-image and constant-image proofs through PC degree two, on residual
boards with N>=3. The proof clears incompatible cells and obtains an empty row;
it uses no same-row exclusions and does not assume a multiplicative design.

The bit-label family extends the earlier one-column common-partition control
to every column. It has O(n log n) sharply Boolean affine inputs and minimal
exception count n^2, persisting after arbitrary matching restrictions. However,
placing each column's tuple in its own ENS block makes every block eligible for
the existing single-column normalizer, at any positive accuracy. This is a
control on freezing-based coverage, not a lower bound against all elimination.
It is not asserted to be forced by the actual Frege source inventory.

Dependencies, read in their exact relevant forms:

- [Proper-normal-form recognition and earlier label control](https://kbr-.github.io/math-research/#partition-statistic-recognition).
- [Common-partition cell clearing](https://kbr-.github.io/math-research/#partition-freezing-completed-rows),
  used as the template for the new direct probe obstruction.
- [Single-column normalizer and original-degree transfer](https://kbr-.github.io/math-research/#column-state-normalizer).
- [Archived finite normalizer evidence](../column_state_normalizers_20260912_46/checks-01.jsonl),
  retained as prior evidence, not rerun in this cycle.

The zero/occupied states used in recognition satisfy the proper column ideal,
not the full PHP system. The explicit bit Booleanity identities and the
separating-profile contradiction supply analytic checks without an extra suite.

Preparation includes the previous checkpoint's public-history audit and two
review-denied push attempts, despite the user's current standing authorization.
The mathematical phase includes the exact source reading, proof, drafting, and
a short outline of the next normalizer-output profile question. Two guessed
source-range endpoints overread material; one capped read produced a broken-pipe
diagnostic. The proposed small excerpt helper addresses that concrete friction.

The timing fragment, archived session, and provenance manifest preserve the
measured interval and the identity of the reused normalizer evidence.
