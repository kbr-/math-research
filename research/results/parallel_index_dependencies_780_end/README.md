# Parallel dependency inventory: positions 780 through 865

`patch_780_866.json` is the final immutable proposal for this assignment; `patch.json`
is the same final content. There are 85 reviewed source-relative relationship
inventories and 357 typed scoped edges. The existing reviewed exponential bit-PHP
publication seed at position 860 is preserved, not overwritten. Original claim
text and all status/topic/significance/formalization metadata remain untouched.
`build_patch.py` reconstructs the proposal and exact source fingerprints.

The worker used the saved equation-inclusive candidate inventory, compact claim
metadata, then targeted owning proof or context sections to distinguish direct
prerequisites, applications, comparisons, repairs and conjectural conclusions.
Shared Lean module ownership was split by declarations; unrelated imported helper
names did not become mathematical dependencies. Context-review citations are
explicitly `cites`, not proof prerequisites. External inputs use existing local
source audits, or the exact notebook invocation of a standard theorem, without
inventing a paper URL or asserting a new literature audit. No new Lean verification
or historical numerical rerun was performed.

## Mathematical-audit flags

Relationship review identifies recorded proof roles; it does not certify the
truth of every referenced premise. Three exact source uses were reported promptly
to the coordinator's independent negative-association audit:

- Position 783 (`lem:pair-space-round-bound`), H1: the proof uses decreasing
  events on distinct columns of a uniform injection to infer exponential avoidance.
- Position 809 (`thm:single-row-wide-readers`), D1: increasing events on distinct
  rows of a uniform bijection are asserted negatively associated. The low-mass
  matching-number part is separate and uses no such assertion.
- Position 811 (`obs:pinned-wide-mass-barrier`): the proposed repair imports D1
  after conditioning on pinned images, in addition to its already open
  concentration and alive-pin counting obligations.

Conditional composition theorems accepting `p_R` remain distinguished from
corollaries instantiating `p_R` using the affected round arguments. Reordered-tree
or decoder repairs do not silently fix probabilistic premises. The coordinator
owns new dated mathematical audit entries and any propagated status changes.

## Suggested record paragraph

The final historical index range received 85 source-relative relationship
inventories and 357 scoped edges, while its existing reviewed publication seed was
preserved. The review distinguishes conditional composition from unconditional
round-bound instantiations, conjectures from degree-two theorems, finite checks
from general results, and generic-CNF bridge declarations from later declarations
in their shared Lean module. Three direct uses of the disputed permutation
negative-association premise were reported for the separate mathematical audit.
No mathematical source entry or proof was rewritten by this worker.

## Validation and measurement

`integration-validation.json` records a temporary in-memory merge into the live
registry: schema validation passes and 9,371 source/endpoint references resolve,
with no errors. All 85 claim IDs and all 357 edge IDs are unique, and every current
target exists. This was a dry-run validation; the canonical registry was not edited.
The source evidence has per-anchor hashes for coordinator freshness checks.

Timing session `parallel_dependencies_780_end` is exported to `timing.html` and
archived under `research/provenance/session-records/`. Timing is worker elapsed
wall time; overlapping worker intervals must not be summed. No Git commit or push
was performed by this worker.
