# R24 dependency: actual initial clauses

Extract and verify compact pair-clause value certificates at2h+ℓ and semantic
coverage of actual usual bit-PHP CNF axioms. Use pair literals b_it+b_i't and
label-disagreement CNF literals b_it+z_t, b_i't+z_t; the old compact axiom is
the product of complements, not an affine equality axiom. A new finite indexed
literal-product prefix helper provides degreeℓ−1 cofactors (empty index set
included). Its full proof shares this new dependency's introducing record.

Dependencies R18 registry, R02 PC reuse/NS inclusion, original compact base,
finite affine-map polynomial bridge. R24 global source-DAG normalization and
node/inventory bounds remain unproved by this local dependency checkpoint.
Preliminary source/API reading and proof planning preceded this clock.
