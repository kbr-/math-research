# Whole-value failure of the cheap-input invariant

The [notebook proof](https://kbr.is-a.dev/math-research/#entry-2026-09-20-whole-value-input-window-failure)
contains the complete binary source construction, a weighted confinement
certificate, the residual-board lower bound on its zero-witness degree, and a
positive control eliminating a singleton child without any degree loss.

All bottom blocks have rank-one decoded literal inputs. The later input is a
genuine whole parent value, not a raw coefficient expression. The retained
system is still nonrefutable at the larger cutoff. The counterexample refutes
persistence of the sufficient cheap-input condition, not the single-parent
criterion itself or the desired complete-source endpoint.

No new numerical experiment was needed: the ordinary NS certificates and
constant restrictions are explicit. The potentially large expansion of the
confined PHP row product is a degree argument, not a small-size proof claim.
The metadata checker is not an independent mathematical verification.

The first metadata registration attempt failed on a mistaken claim-ID prefix
(thm instead of prop); it made no registry change. The corrected retry passed.
Working context was reviewed and retained at 1,331 words, within the hard
limit, to preserve the active setup and exact correction scopes.
