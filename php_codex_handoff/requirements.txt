# For historical numerical checks, not for reading/import metadata/timing.
# Keep original finite-field arithmetic; these are not floating-point rank tools.
numpy
# Only A01 (annihilating_functional_checks) additionally needs:
numba
# Optional PDF-to-text fallback for imported references:
# pypdf
