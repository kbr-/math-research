# Retained label-parent source audit

Full statements, certificates, and limitations are in the
[notebook entry](https://kbr-.github.io/math-research/#entry-2026-09-13-source-label-indicator-audit).
This is an algebraic source audit, with no new numerical experiment.

The audit applies the earlier truth-table characterization to the actual
compact preprocessing. Label bottom blocks pack affinely; their parity parents
reproduce unary affine queries. Decoding label indicators through degree ell
then normalizes this two-level subfamily back to the unary affine family without
increasing the original supplied NS or PC refutation degree.

The earlier multiscale family can be normalized by changing constants so its
parent ORs are nonconstant. Its residual linear-rank event is unchanged.
This carries the existing numerical-method obstruction into retained source
syntax, without constructing a Frege proof using that inventory essentially.

Conditioning on one bit separately halves a one-row label span's transported
rank. Thus the unconditioned unary rank criteria cannot be reused automatically.
Other nonlinear flat-indicator inputs and general multilevel elimination remain
open.

The source map records exact local anchors, their roles, and the symbolic
degree ledger. Check these against the complete proof in the notebook.
Historical image computations were not rerun or presented as new evidence.
The provenance manifest, timing fragment, and session archive retain the audit
checkpoint and measured work.
