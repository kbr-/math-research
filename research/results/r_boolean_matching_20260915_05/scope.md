# R14: cube/residual dual separation

Target lem:cube-residual-dual-separation, exact notebook anchor cube-residual-dual-separation and paper sections/04-decoder-separation.tex Theorem row-separation. Required entire claim: ell≥2,n=2^ell,m=n+1,1≤k≤B,4(k−1)<n,n≥2B−1, every nonzero f∈rowLinearSpace F2 m ell k has a functional mu with mu(compactDecoder f)=1 annihilating functionalUnary PC throughB; consequently decoded f and f itself are outside corresponding PC spaces. Must handle constants and actual degree t<k, with actual residual design and complete old-system annihilation. The argument may naturally work for arbitrarym using generalized R10; retain the original theorem as an application.

Available verified dependencies: R05 Boolean reduction, R09 full matching-moment correspondence, R10 matching extensions/PC=NS for arbitrarym, R12 compactDecoder coordinates/exact degree/injectivity and compactDecoder_PC, R13 MathResearch.rowLinearSpace and rowLinearSpace_top_coefficient (chosen RowMonomialIndex x, coefficient nonzero, x.1.val=f.totalDegree). Current integration pin d202b7ff4b720ce49f00f238309be08307728570.

Proposed decomposition:
1. Disjoint coordinate-pair selection. Flip a label in one specified bit. For a forbidden vertex set F, F union flip(F) has size≤2|F|, hence an unused pair exists if2|F|<n. Greedy selection yields pairwise disjoint pairs for t directions under4(t−1)<n. This avoids dividing by two and directly counts bad vertices.
2. Residual/tensor annihilator. May use a finite sum of cube restriction homomorphisms instead of manually defining each matching moment. Prove the sum loses at least one ordinary degree per selected row; each unary generator restricts to zero or a fixed residual generator, so this supplies an actual ordinary NS annihilator throughB from a residual design throughB−t. If that interface is harder than the source moment construction, use the source's exact tensor moment and marginal proof instead. Either way no abstract separating functional assumption is permitted.
3. Row-linear coefficient isolation through the exact decoder. Pair endpoint sums isolate the chosen bit in each selected row. Missing selected rows cancel, and max actualdegree t forbids extra occupied rows; the chosen coefficient is1 overF2. This must recover the actual R13 rowLinearSpace, not assume a narrower custom space.
4. Parameter closure, residual-column positivity, normalized residual design and final PC exclusions via R10/R12. A t=0 uniform construction may cover constants if it simplifies proofs; otherwise give a separate checked constant case.

New reusable dependencies get indexed files and full MathJax proofs in their introducing records. Parent owns living sections/route; no independent rebase or push. The post-all-Rxx generalization sweep is deferred. A notebook-excerpt read initially failed on transient conflicted HTML during parent integration; paper and exact bounded notebook source lines were read instead, and parent confirmed parser succeeds after integration. This is not a mathematical gap. Some initial R14 design before this cycle was recorded as preparation in the previous checkpoint interval; no timing is backfilled.
