# Failed balanced and parallel replay attempt

The full argument is in the notebook entry
[Balanced proof replay](https://kbr-.github.io/math-research/#entry-2026-09-12-proof-dependent-batching-attempt).
The task targeted the open source-family elimination bridge and stopped at the
exact missing inference. No new general elimination theorem was obtained.

The one-block proof learns inputs from the base plus the other blocks.
Independent applications therefore produce conditional derivations, while
balanced composition still adds the existing degree charges. Nested-span
learning avoids this for a supplied structural reason; polynomial family count
does not provide that reason.

A Boolean-grid control with two independent blocks shows that a selector for
one input need not supply domain proofs for the other block's weighted
companions under any scalar coefficient assignment. The control is satisfiable,
uses only domains, and does not purport to refute a PHP-specific transfer.
It applies the previously recorded constant-normalizer degree argument.
The boundary h=t provides a successful scalar normalization control.

Exact dependencies read:

- Historical one-block elimination, additive composition, and equal-span
  learning in chapters/07_elimination.md.
- Historical nested-span and core-residual proofs in chapters/08_batching.md.
- PC reuse, substitution, field reduction, and selector identities in
  chapters/01_foundations.md.
- The notebook's target-annihilator batch, scalar-support control,
  assignment-tree count, and small-probe Boolean-grid control.

The historical chapter paths are relative to
php_codex_handoff/manuscript/. Source hashes are in provenance.json.
No numerical computation, source download, or broad verification suite was needed.
The claim index gains no theorem for this failed attempt; its existing open
bridge references the full record.

The central gap is unchanged. The next task checks a different interface:
whether an explicitly stronger auxiliary PHP base has the needed PC degree
lower bound and makes a restriction route applicable to actual bottom OR inputs.
The original weak source encoding and the target theorem are not changed here.

The initial preparation interval includes finalizing the preceding audit and
committing its small framework improvement. Reading includes interpretation;
the mathematics interval includes the failed mechanism search and entry writing.
The timing fragment and archived session preserve actual measured intervals.
No extra framework change was warranted after applying the new stopping rule.
