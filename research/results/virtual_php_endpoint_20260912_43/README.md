# Ordinary-PHP endpoint for the new value interpretation

The complete proof and ledger are in the
[notebook entry](https://kbr-.github.io/math-research/#entry-2026-09-12-virtual-php-endpoint).
This analytic cycle applies the existing affine endpoint to the new completed
PC value proof. It does not assert a base-only refutation.

## Construction

For n>=2, protect the designated final PHP root and clause-evaluation subtree
from virtual-boundary selection. Shared protected objects remain genuine.
The new source compiler then proves the ordinary final product Z through
D <= (c+2d)L, with c=max(M,10,p-1).

Apply the old truth change q_ij=1-x_ij and one global affine map: zero the final
root coefficients; give each row block its first all-ones row; give each ordered
collision block first coefficients (1,x); zero their later rows. Extra original
propositional variables outside the target can be assigned Boolean constants.

The target becomes one. Final companions become row/collision generators;
clause companions become base multiples through degrees two or three; removed
field equations become zero or x^p-x with their degree-p Boolean certificates.
Every image proof fits the original degree of the used axiom. All remaining
ENS inputs are specialized, including any later consumers of the final root.
Hence final-root freshness is unnecessary for this valid-family transfer.

The destination is exactly the weak PHP base with Boolean domains, linear row
equations, and distinct-pigeon column collisions, plus valid ENS. Same-row
exclusions are never added. The final PHP and designated clause families are
removed, together with the previously selected virtual axiom-boundary families.

## Degree, levels, and remaining family

The PC degree stays at most (c+2d)L, with d<=ell' and
L=(ell'+1) max(p-1,h)^ell'. At fixed p,h,depth and source basis the bound is
independent of proof size; the family still encodes the particular source proof.
At logarithmic h and polynomial source size, degree is polylogarithmic and
family count polynomial. An inventory of T formula nodes has at most T OR
objects and T^2 flattened input slots before sharing or deletion.

The remaining family has at most d levels. The earlier signed construction
has its separate highest-source-level omission and degree K0+2pHL. These are
different constructions; the new degree is not combined with an unproved
highest-layer omission.

The clause images are sharp old-base-zero values, and the other values remain
in the constant/genuine/negation/MOD Booleanity portfolio. Thus the existing
same-degree level-ordered rank and equal-span pass is available after the
endpoint. It leaves literal input bases of rank above accuracy, spans excluding
one, and distinct spaces at each level/accuracy. No ranks of an unspecified
Frege proof were computed. Essential wide support and affordable elimination
remain open.

## Evidence and measurement

The exact direct-root and clause identities were reread in targeted notebook
excerpts. They already supply the needed degree-preserving image certificates;
no new numerical or rendering suite was warranted. The new source construction
and strict-support result provide the other dependencies. All full proofs remain
in the notebook and their original dated records.

`provenance.json` hashes the source-construction and strict-support result records,
the shared PC kernel, this README, and the claim index. Final timing is embedded
in the notebook; command evidence is archived under
`research/provenance/session-records/virtual_php_endpoint_20260912_43/`.
The final source read and planned mathematics marker ran in the same awaited
tool call. No new dependency or framework rule was added.
