# Assigned H04–H07 scope

The assignment is Spin-formalize restricted to H04–H07 of the bit-PHP route.
H01–H03 are reused checked dependencies. H12–H13 and R claims are excluded.
This first bounded cycle proves the H04 cone contraction and exactness, with
explicit empty-face augmentation. H05 will use the same contraction in degrees
where its filler cannot be the full simplex. H06–H07 require a finite augmented
cover chain chase; their exact interface is coordinated with the chessboard agent.

A cone vertex means every face remains a face after adjoining that vertex.
ExactAt K k quantifies every supported k-cell cycle and asks for a supported
(k+1)-cell filler. AcyclicThrough K n asks for exactness for all k<n: this is
reduced vanishing from degree -1 through n-2, without truncated negative bounds.
The contraction adjoins the cone vertex when absent, with coefficient formula
h(c)(S)=c(S minus v) if v belongs to S, and zero otherwise. The full identity
boundary h(c)+h(boundary c)=c will be proved before exactness.

The rule files and computation policy were read before timing began; subsequent
context restoration and formalization are instrumented. Existing paths stay
stable pending the parent's provenance audit; supporting interfaces here are
project adaptations of standard elementary cone contraction, without novelty claims.
