# Full R24: actual bit-PHP clause DAG to ordinary PC

Target entire indexed thm:bit-PHP-affine-clause-PC-transfer, for both compact
pair-difference initial clauses and the actual usual CNF initial clauses.
The source syntax explicitly includes ordinary complementary resolution,
semantic weakening, and the stronger binary-semantic convention; nodes form
a topological list with unrestricted reuse. An empty finish node may be any
node in the counted S-node DAG.

Instantiate the generic DAG controller with strictly ordered pigeon pairs.
Prove their cardinality binom(m,2), and use the exact local actual-CNF cover
and compact initial-value proof at2h+ℓ. All supplied initial-PC/controller
hypotheses are discharged. The empty compressed final subclause has value1.
Conclusion degree max(2h+ℓ,4h+1), N≤3S+binom(m,2), actualwidth bound
W=max(ℓ,mℓ+2), totalinputs≤NW, totalvariables≤mℓ+hNW. These explicitly imply
polynomial input/coefficient inventory; do not claim auxiliarywidth≤mℓ+1.

Also identify registrySystem with the existing ensFamilySystem on the same
Σ-tagged variable type, using canonical fresh-block renaming, so R17/R16 can
consume the actual endpoint. Unit-span clauses remain allowed and are handled
by the checked unit-or-literal family-removal generalization. No asymptotic
lower-bound closure or R25 theorem is included here. Preliminary API reading
and assembly planning preceded this clock.
