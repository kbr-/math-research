# Level-ordered virtual axiom boundaries

The complete construction is in the
[12 September notebook entry](https://kbr-.github.io/math-research/#entry-2026-09-12-virtual-axiom-boundaries).
This is an analytic source-interpretation theorem, not a coefficient substitution
applied to a completed ENS proof. No new mathematical suite was needed.

## Construction and scope

Start with a finite ordinary source-evaluation inventory for the recorded
Boolean/MOD presentation. Select any recognized axiom-boundary objects, retaining
protected objects as genuine products. Positive/negative recognitions prescribe
virtual zero/one. Sound recognition cannot require opposite constants for the
same underlying formula. A shared evaluation object receives one choice;
independent copies can be selected separately or kept genuine.

Process structural OR levels in order. A level-s object's flattened inputs
are already defined below s. Unselected objects receive genuine ENS families
on those rebuilt inputs. The strict leaf theorem supplies each selected
object's new witnesses over the completed lower system, before any higher
level is constructed. Proper same-level OR groups are bypassed as inputs.

The original structural majorant L is preserved by the value constructors.
This is not a claim that every old collected degree or cancellation is preserved.
There are no new ENS families or structural levels. All later inputs are
recomputed, so removed coefficient variables do not survive in old cached
ancestor polynomials.

With c=max(M,10,p-1), where M bounds the fixed Boolean frame leaf counts,
the common annihilator/unit bound satisfies

    T_0 = 0,
    T_s <= max(T_(s-1)+2L, cL) <= (c+2s-2)L.

At maximum level d>=1, the recorded source value compiler therefore has degree
at most (c+2d)L. Protected genuine axiom leaves still have their compiled value
proofs. Extra assumption leaves would need their own supplied proof ceilings.
This does not remove every remaining highest-level proof-line block or bound
the essential wide family.

Constants, genuine products, and literal negation/MOD also retain the existing
sharp NS Booleanity induction at twice the current value degree, confined to
the current structural level. That fact does not convert the virtual unit or
annihilator witnesses into same-degree NS certificates.

## Applications, dependencies, and timing

The notebook includes the positive A-or-not-A-or-C application and the negative
outer MOD-recursion boundary. It also specifies actual retained consumer inputs
after a child becomes zero or one; a consumer of (0,y) remains nonconstant.
These are analytic consequences of the construction, not unreported finite runs.

The proof uses the preceding strict-support criterion and conditional value
compiler. `provenance.json` hashes their result records, this README, and the
claim index. The full statements are in the notebook; no handoff or paper was
reimported. The working-context section was consolidated around the current
interpretation, retaining links to earlier tools and controls without changing
the append-only research record.

The final timing is embedded in the notebook and complete command evidence is
archived under `research/provenance/session-records/virtual_axiom_boundaries_20260912_42/`.
Preparation includes the preceding research and timing-rule checkpoints, their
public-history audit, and push. No new dependency, rendering check, or further
framework rule was introduced.
