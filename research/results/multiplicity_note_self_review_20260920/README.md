# Version-1 preprint self-review — 20 September 2026

This is an in-place review of the uncirculated first version, not a new public
version. `checklist-review.json` records all 26 dispositions, and `sources.json`
records the bounded primary-source comparison. The full argument is unchanged.

The final PDF was built in three LaTeX passes after all source edits. An earlier
checked build was superseded when the author requested a direct development-history
link; its command output remains in the timing archive. Complete
logs, extracted text, PDF metadata, fonts, hyperlinks and automated layout checks
are retained here. `verify_pdf.py` reproduces those checks through `compute.sh`,
using the existing Poppler and Pillow installation; it overwrites reports in this
directory, so later versions should use a new evidence directory. Temporary page
PNGs are not published. Automated checks do not claim independent human visual
review. The initial draft's evidence remains in its earlier dated directory.
