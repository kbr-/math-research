# Literal naming and the actual source-feasibility obligation

The [notebook review](https://kbr.is-a.dev/math-research/#entry-2026-09-20-literal-naming-source-review)
records the exact costed equivalence after naming old bits, and proves that the
E=D-L core still extends when literal names are substituted away before the
original completion and restored afterward.

This is an application and audit of the existing affine-definition replay and
all-prime active-window theorem. It does not prove a new lower bound. Naming
moves arbitrary old-parity expressions into relations among named values, so
row-local visible syntax alone cannot be a sufficient feasibility invariant.

No new numerical computation was needed. The three-variable Boolean guard is
fully specified in the notebook: its core has all nonconstant moments zero,
while a proposed outer moment x1*x2*y=1 violates the degree-three multiple of
y-x1. The correct substitution completion assigns that moment zero. It checks
the stated completion rule symbolically, not a PHP instance.

The notebook includes the complete telescope and cutoff arguments, exact named
core equations, dependencies, scope limits and the revised next task. Metadata
checks and measured operational evidence accompany this record.
