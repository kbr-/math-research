# Index curation foundations

Baseline revision: `c289a353136c262e04fe754846ef063144f9acf9`.
The [notebook entry](https://kbr.is-a.dev/math-research/#entry-2026-09-19-index-curation-foundations)
records this bounded checkpoint, not completion of the full curation plan.

- `baseline.json`: population counts before enrichment (866 claims, all new fields empty).
- `curate_seed.py`: explicit first source-reviewed batch for the telescoping claim,
  publication theorem and exponential corollary. Run from this checkout with the
  protected launcher after `claim-index.py upgrade`; it preserves original text
  and names the exact source evidence, scopes, declarations and relationships.
- `coverage.json`: full per-field report; three claims reviewed, 863 unreviewed in
  each field. Other claims have not been claimed reviewed through inference.
- `validation.json`: all 1,326 original and added evidence references resolve.
- `claim-inventory.tsv`: complete untruncated ID/summary inventory generated by
  `tools/claim-index.py list --fields id,summary --format tsv --out PATH`.
- `provenance.json`, `timing.html`, and the session archive: source hashes and
  complete measured command evidence.

Schema v2 adds explicit review states/hashes, multiple formalization scopes,
structured significance and topic definitions. V1 data remains readable. The
archived statements and verification reports were read; no new Lean execution or
literature verification is represented by this metadata pass.

Focused tests cover empty reviewed-field rejection, exact/minimal output,
projection escaping, no implicit listing limit, and staleness caused by relevant
source changes while unrelated appends preserve review validity.
