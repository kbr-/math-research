# R17 assembly scope

Target: the complete indexed ordinary-restriction affine-family audit, including
finite-parameter exclusion and the polynomial-inventory/polylog-degree consequence.
R01–R16 prerequisites, including actual cube separation and unit-span removal,
are already verified and integrated. R24 is also complete for later R25.

Use the actual finite affine-map registry, original polynomial degree, and all
Boolean/companion axioms. Kernel witnesses plus weighted removal give a nonzero
old row-linear PC consequence; the checked cube separator excludes it.
The source finite theorem explicitly assumes D >= 2h+1. The asymptotic consequence
can enlarge smaller ceilings. The square-condition kernel allows a convenient
uniform parameter choice; any different parameter proof will be recorded fully.

Verification reuses source-compatible artifacts from finished workers. No broad
re-read or repeated proof audit is planned. Earlier coordination preceded this
clock; waiting during the measured review was marked as overhead.
