% SPDX-License-Identifier: CC-BY-4.0
\section{The deduction}

\begin{proposition}\label{prop:answer}
For every pair of positive integers \(n,k\), every polynomial
\(P\in\F_2[x_1,\ldots,x_n]\) satisfying~\eqref{eq:hypotheses}
satisfies~\eqref{eq:question-bound}.
\end{proposition}

\begin{proof}
The condition at the origin excludes the zero polynomial. Put
\(d=\deg P\) and apply Lemma~\ref{lem:msz} with \(S=\F_2\).
Since multiplicities are nonnegative and \(\F_2^n\) has \(2^n-1\)
nonzero points,
\[
 k(2^n-1)
 \le \sum_{a\in\F_2^n}\mult_a(P)
 \le d\,2^{n-1}.
\]
Dividing by \(2^{n-1}\) and using integrality of \(d\) gives
\[
 d\ge\left\lceil 2k-\frac{k}{2^{n-1}}\right\rceil
   =2k-\left\lfloor\frac{k}{2^{n-1}}\right\rfloor.
\]
\end{proof}

Thus the polynomial inequality in the published Question~4.3 has an
affirmative answer, without the assumption \(k\ge2^{n-2}\).
The upper bound on the origin's multiplicity is used here only to
ensure that \(P\ne0\). In fact, the same argument applies to any nonzero
polynomial with the required multiplicities at the other points.

The purpose of this note is to record this direct implication of the
existing multiplicity Schwartz--Zippel lemma. It does not supply a new
proof of that lemma or a new method for lower bounds, and it does not
address the other covering questions in~\cite{BBDM23}.

\paragraph{Assistance disclosure.}
The literature comparison, identification of the implication, and drafting
of this note were carried out with substantial assistance from GPT-6 Astra
in Codex, under the author's direction. The source comparison and deduction
are recorded in the
\href{https://github.com/kbr-/math-research}{Noemesis research repository}.
