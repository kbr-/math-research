# Correlated literal-copy control

The [notebook entry](https://kbr.is-a.dev/math-research/#entry-2026-09-15-correlated-copy-moment-control)
contains the complete source, original-cost ledger, scalar-component
contradiction, and correlated pullback.

The test rejects a joint-extension ansatz in which every queried singleton
selector component is a scalar multiple of one freely chosen old functional.
Complete copies of all pigeon bits force those scalars to be Boolean labels;
the copy equations then make the old functional evaluate every collision
polynomial at those labels, contradicting PHP. Higher selector components
cannot repair the contradiction.

This does not obstruct the copy source itself. A complete constant coefficient
map sends each copied product to its old bit and every companion to old
Booleanity. Pullback gives correlated joint moments for every old design.

Parameters and scope:

- n=2^ell, ell>=2, m=n+1, and gamma=(p-1)*ell over characteristic p.
- Each duplicate-literal block has accuracy h, product weight w=2h, and all
  own coefficient-field equations.
- The exact copy link has an NS witness through w+1. Its declared cost nu
  may be larger, including the conservative compiler ceiling 3w.
- The obstruction uses D>=nu+max(1,gamma-1), and only queried singleton moments.
- Old designs exist for arbitrarily large parameters, for example n>=2D-1
  using the matching theorem and cost-preserving affine decoder.
- The local algebra is all-prime; this supplies no new odd-prime full compiler
  and no higher-level source-extension theorem.

No numerical computation was run. Verification is the exact prefix identity,
the active-window moment argument, and the complete coefficient map. Metadata
preserves dependency anchors and focused append/link checks; hashes, timing,
and command output accompany the research checkpoint.
