# Mixed-prefix boundary audit

The [full notebook entry](https://kbr.is-a.dev/math-research/#entry-2026-09-15-mixed-prefix-boundary-audit)
contains the source family, complete ordinary NS identities, parameterized
models, degree tradeoff, and exact limitations.

This audit asks whether fixed source depth bounds the exposed boundary in the
preceding comparison-component compression. Mixed OR/literal ports supply a
counterexample: all prefix blocks are old-affine at one level, but reconstruction
from retained product values alone must retain every prefix. Allowing old-variable
degree delta >= 1 gives r <= k + (k+1)*delta for k retained prefixes.

The obstruction preserves every unweighted mixed port over complete retained
blocks and old Booleanity. It permits arbitrary functions of retained product
values in the boundary-only case. The models retain the actual coefficient
fields over every prime. This is a satisfiable local compiler-interface family,
not a PHP lower-bound instance or a theorem about essential support in all proofs.

## Evidence and reproduction

No numerical job or historical test suite was run. Verification consists of:

- Exact prefix expansion of MIX-witness and MIX-base, with original ceilings
  4h+1 and 2h+2, respectively.
- Complete models at adjacent first-nonzero positions, isolating one prefix.
- A free old-variable cube with constant retained coefficients, giving the
  old-degree lower bound by unique multilinear representation.
- Counting omitted-index runs to obtain the boundary/old-degree tradeoff.

The degree in old variables treats formal boundary product names as parameters;
it is not a new bound for the entire multilevel transformation. The all-prime
algebra is separate from the existing binary full-source compiler.

`check-metadata.json` records the source revision, exact dependency anchors,
focused review, and touched-link/append-only checks. `provenance.json` records
file hashes. The notebook timing table and archived journal measure this cycle;
goal activation and the initial resume-guide read preceded the clock.
