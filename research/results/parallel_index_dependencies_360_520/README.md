# Parallel dependency inventory: positions 360–519

Four bounded source-relative batches close the 160 assigned claim inventories.
The final provenance-resolution patch replaces four initially pending dispositions
after checking exact existing local audit passages. No paper was re-downloaded,
no numerical evidence or Lean proof was rerun, and no canonical registry, notebook,
shared plan or Git state was edited by this worker.

## Integrate in this order

1. `batch1.json`: positions 360–379, 20 reviewed inventories, 52 proposed edges.
2. `batch2.json`: positions 380–419, initially 38 reviewed and two pending, 94 edges.
3. `batch3.json`: positions 420–479, initially 58 reviewed and two pending, 145 edges.
4. `batch4.json`: positions 480–519, 40 reviewed inventories, 127 edges.
5. `provenance-resolution.json`: replaces the four pending inventory dispositions
   with reviewed dispositions and adds eight source-specific edges.

The final union has **160 reviewed inventories and 426 proposed scoped edges**.
These are claim-level mathematical and recorded formal-proof dependencies,
applications, refinements, obstructions and citations. Direct dependencies are
separate from transitive explanations; source syntax/background and unsupported
stronger transfers are not promoted to theorem dependencies. Standard library
internals are outside the claim graph's scope.

Each inventory includes a baseline claim fingerprint, source evidence hashes,
a reviewed rationale and no unresolved next action. Edges carry their specific
proof/application scope and source location. Merge edges before regenerating the
incident relationship-review hashes. Refresh affected targets under the canonical
editing contract; an incoming reviewed edge does not finish an otherwise incomplete
target's outgoing inventory. Preserve all unrelated field reviews and mathematics.

The source baseline is `dc1d95b4cb6a463436256b830afd69029c1dc3ed`.
`validation.json` confirms every current claim fingerprint and inventory evidence
hash, every edge evidence target, and schema validity of an isolated union with
the current registry. No existing edge-ID conflicts were present at this check.
This structural check is not a new mathematical correctness or literature audit.

## Review scope and qualifications

Compact source packets from the classification batch supplied orientation.
Candidate extraction identified explicit links, equations and named Lean uses.
Targeted full passages resolved actual proof roles, shared headings, scope
comparisons and unlinked earlier arguments. Named formal dependencies were checked
against actual declaration uses, not merely imports. In particular the verified
binary semantic-cover result is distinguished from its additional informal PC
corollary, and the all-field matching proof uses a later elementary filling
replacement rather than treating binary Lean coverage as all-field verification.

Finite records mostly apply constructions while their complete saved outputs are
the direct evidence. Two explicit exceptions in the first record are its reused
local polynomial template and analytic six-by-six separator minimum. Purely local
algebraic/countermodel arguments have reviewed empty prerequisite inventories when
appropriate. Novelty remains separately tracked by the classification pass.

The provenance resolution links the compact decoder's lower bound to the exact
Razborov audit, balancing to the actually read BIKPRS concluding proof, and the
small-characteristic subset-sum objection to HLT v1 Proposition 21. Unread book
material is not imported; abstract-only literature screens remain citations.

## Reproduction

Each `batchN.py` and `resolve_provenance.py` supports `--out PATH` and writes only
an isolated patch. `validate_patches.py --out PATH` validates their union without
canonical writes. Run these small processing jobs through `compute.sh run` with
one thread and category `local_processing`; the worker session is
`parallel_index_dependencies_360_520`. Scripts 2–4 and the resolution script reuse
the common builder prefix of `batch1.py`.

`timing.html` and the archived session preserve actual worker time. These intervals
overlap the coordinator and other workers and must not be summed into total wall
clock. Initial source discovery commands included an absent directory glob and one
wrong source-anchor lookup; neither changed files or affected mathematical evidence.

## Suggested notebook record

Parallel dependency curation closed 160 further claim-level source inventories,
adding 426 proposed scoped relationships before global integration. The pass spans
source modules, compact-bit rigidity and moment bands, normalization and matrix
program obstructions, the binary affine-clause chain, and all-prime extensions.
Explicit proof prerequisites are separated from applications, historical/source
attribution and failed generalizations. Exact local audit lookups resolved four
initial external-provenance questions without repeating paper retrieval. The pass
changes navigation and provenance only; no new theorem or proof verification is
claimed.

Timing limitation: some batch-patch drafting occurred before its phase marker was
executed and is therefore included in the recorded reading interval. The raw
measured categories are retained rather than retrospectively reallocating time.
