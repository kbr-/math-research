# Nonzero weighted-port reuse audit

The [notebook entry](https://kbr.is-a.dev/math-research/#entry-2026-09-15-weighted-mixed-port-reuse)
records the complete mask identity, actual tautology-frame coefficients,
ordinary NS degree budget, and unchanged global obligation.

For a mixed prefix comparison H=c-ab with a retained and c replaced by zero,
the nonzero mask 1-a gives (1-a)*(-ab)=b*(a^2-a). Its retained Booleanity witness
fits the original cofactor-plus-port budget. The frame not A or (A or B)
produces exactly this mask. The earlier pure-disjunct virtual-interface proof
already covers the application, so no new source coverage is claimed.

Independent replacements for separate summands are insufficient; a common
substitution or compatible source-dependent functional is still needed.
The first mixed-band certificate-support theorem is not extended to the full
source by this audit. Old PHP residuals were already allowed by the existing
relative interfaces.

No numerical computation or historical suite was run. Verification consists
of the displayed exact identities and degree accounting. The metadata records
the prior source revision, exact dependency anchors, and touched-link and
append-only checks. Hashes, timing, and command output are preserved alongside
the notebook checkpoint.
