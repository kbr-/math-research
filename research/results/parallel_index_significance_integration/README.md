# Significance integration dry run and relationship reconciliation

`patch.json` normalizes all **nine** significance-audit proposals against the
live registry. It changes only each selected `significance` value and its review.
`report.json` records source/evidence checks and an equality check proving every
other field, claim, text cell, formalization record and relationship was preserved.
The dry run validates in memory and never writes canonical metadata.

Seven proposals supplied evidence hashes; every supplied hash matches current
sources. The two calculus proposals supplied locator lists instead, so their
notebook excerpts were additionally compared with their immutable declared Git
baseline, then all report/statement evidence was fingerprinted at integration.
The exact producing proposal files and hashes are retained in the patch.

Eight novelty dispositions remain `unknown` after completed bounded comparisons.
The binary predecessor inherits the existing publication theorem's `candidate`
novelty and preprint status as the same result, not a second discovery. Four
normalizer claims are useful general tools; the heterogeneous frontier, odd-prime
explicit calculus and two related query barriers remain publication candidates
with precise follow-up actions. No stronger calculus equivalence, exhaustive
priority certification, expert correctness review or new proof verification is
asserted. This integration reviews the audit reports and scope; it does not
independently repeat their web searches.

Reproduce with protected execution of `dry_run.py --out PATH`. It fails if source
text or supplied evidence changed and proves unrelated-field preservation before
saving output. Apply the resulting fields/reviews only; root owns mathematical
status changes.

`relationship-scope-decisions.json` reviews all **25** same-edge scope collisions
between the earlier dependency patches and the 37 duplicate/refinement proposals.
All preserve the existing scope; the other **12** edges are new. The merger worker
was notified directly. Preserve the conditional switching qualification and new
wide-round probability hypotheses in claim metadata; the supersedes target still
requires its normal status review. Root owns canonical integration and Git.
