# Reserved correction endpoints: scoped status review

`status-proposals.json` covers exactly the 15 `coordinator_reserved` claims in
`parallel_index_correction_endpoints/status-review-proposals.json`. It preserves
the ten currently conditional statuses and proposes conditional for five remaining
aggregate/corollary records:

- `cor:multirow-size-switching`: avoiding H4 does not avoid the affected H1 premise.
- `cor:crowded-size-switching`: the count reduces to the affected exposed theorem.
- `thm:two-role-few-rows-switching`: K1 has an additional **direct** product-over-
  bijection-columns use, reported immediately to the coordinator.
- `cor:two-role-size-switching`: clause 1 inherits H1; clause 2 the exposed theorem.
- `thm:pair-space-composition`: Theorem D's supplied-`p_R` implication remains;
  Theorem F's use of Lemma E/H1 has the probability gap.

Each proposal includes the exact original/correction/audit anchors, source hashes,
a review note and a named remaining mathematical obligation. The correction to
rank constants and tree conventions is separate from the new probability audit.
Later decoder/composition repairs cannot supply an unproved probability premise.
No final high-density reader statement is declared false, and no probability repair
was attempted. The canonical index and notebook were not edited.

The source sections for the newly identified use and all size reductions were read
in full; the earlier direct-use audit, exact rank correction and compact/slack
composition correction were read against the recorded scopes. The pair-space and
current-term proofs had already been read in the worker's preceding assigned batch.
`review.py` reproduces the patch and checks exact equality of the 15 assigned IDs.
If the coordinator appends to the still-open audit entry before applying these
reviews, refresh that audit evidence hash rather than retaining a stale digest.

Timing is exported and archived as `parallel_correction_scope_review`.
