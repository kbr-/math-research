# Parallel metadata review: positions 520–679

Baseline: `dc1d95b4cb6a463436256b830afd69029c1dc3ed`.

Reviewed 160 existing indexed claims using verbatim indexed summaries/assessments
and bounded notebook packets. Proposed status, topics and significance for 159;
preserved the previously reviewed publication theorem at position 553 in full.
No original text, formalization disposition, dependency inventory, canonical
registry, notebook, Git state or live route was changed by this worker.

`patch.json` contains explicit per-ID values, source targets and review notes;
root integration should call `make_review` against the combined registry so
review hashes include its final topic definitions. `build_patch.py` regenerates
the proposal. `check_patch.py` applied it in memory and validated 477 field reviews.
`packets.json` preserves the selected source excerpts and their scope limits.

The batch covers old-target/moment preservation, restricted two-level ENS source
families, Boolean-domain normalization obstructions, topological formalization
interfaces and ordinary polynomial foundations. Mathematical status follows the
recorded scope; finite controls remain finite. Known classical inputs retain
attribution, project APIs do not acquire independent novelty, and historical
publication audits remain context rather than a new literature verdict. Complete
formalization here means the previously recorded exact scope, not a new replay.

No new novelty claims or publication-ready verdicts are proposed. The historical
publication audit and draft entries do not replace the current theorem/revision
record. Relationship inventory completeness remains entirely unreviewed by this
worker and must be handled by its assigned reviewers.

One protected extraction invocation could not access user systemd in the sandbox;
the escalated retry succeeded. Initial policy reading preceded instrumentation.
Worker intervals overlap other agents and must not be summed as serial wall time.
