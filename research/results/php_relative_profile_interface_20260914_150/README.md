# PHP-relative source-interface audit

The [notebook entry](https://kbr.is-a.dev/math-research/#entry-2026-09-15-PHP-relative-source-audit)
records the full argument and its limitations. It finds no new general source
compression. Existing profile, input-replacement, and learning theorems already
permit old PHP residuals; the missing work is constructing useful witnesses at
a sufficient cost on the full source.

The new formal-input observation says that old-base-certified changes cannot
alter the constant selector coefficients of an affine-in-selector input below
the old refutation degree. A related forward specialization sends formal old
targets into the old ideal plus the input space's affine intersection.
These statements concern independent formal selectors and explicitly do not
justify assigning genuine products arbitrary affine values or discarding raw
coefficient witnesses.

The column-state idea was recognized as an existing mechanism before another
computation was written. Its compact label-indicator calculation is retained as
a reused PHP-relative control. A sparse collision-support condition was also
checked against the actual boundary and does not address its globally supported
residuals.

## Exact repository sources

- `costed-ns-value-profile` and `costed-profile-product-closure`;
- `costed-profile-unbalanced-PC-compiler`;
- `full-base-relative-learning` and `quadratic-PHP-relative-interface`;
- `input-reduction-transfer`;
- `column-state-normalizer`;
- `selector-weighted-degree-reflection` and `selector-degree-reflection-scope`;
- `signed-cube-row-linear-separation`;
- `affine-intersection-original-degree-gap`;
- the complete cycle 149 Boolean-domain obstruction.

These are existing notebook anchors, not newly verified literature results.
The formal-input proof and its NS degree accounting are given in the new entry.
No new numerical suite was required.

## Targeted literature scope check

Jiaqi Lu, Rahul Santhanam, and Iddo Tzameret,
*AC0[p]-Frege Cannot Efficiently Prove that Constant-Depth Algebraic Circuit
Lower Bounds are Hard*, arXiv:2509.16824v1 (20 September 2025):
https://arxiv.org/abs/2509.16824v1

Only the versioned abstract and scope were checked. The abstract distinguishes
its DNF family's proof-size claim from the still-open question of whether that
family consists of tautologies. It does not provide a lower bound for a known
ordinary-PHP family. No theorem from the diagonalization proof was imported and
no PDF or full text was copied into this checkpoint.

## Evidence and process

The user-requested generic falsification guidance was committed separately as
`83d079f`. The active goal was updated to execute the Spin prompt in PROMPTS.md;
the current prompt was reread and the same research assignment continued.

`check-metadata.json` records the focused proof/source and touched-link review;
`provenance.json` hashes this note and that evidence. Timing and full command
outputs are archived under the session label in
`research/provenance/session-records/`. The session began on 14 September;
the notebook checkpoint is dated 15 September.
