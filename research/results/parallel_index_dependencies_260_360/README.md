# Parallel dependency inventory: canonical positions 260–359

Completed source-relative relationship inventories for all 100 assigned claims.
Ten `patch-LO-HI.json` files contain claim/source fingerprints, explicit review
notes and 244 scoped relationship proposals. Of these, 240 IDs were new at the
validation snapshot. Four existing IDs need scope reconciliation by the
coordinator; they are listed in `validation.json`. Existing accepted edges were
preserved during proposal validation, never automatically replaced.

This worker changed no canonical metadata, mathematical record, shared plan,
workflow file or Git state. The coordinator owns integration and checkpointing.
The final regenerated patches supersede the earlier snapshots communicated during
work: all linked record anchors are now retained as edge evidence. In particular
the parity-attack rediscovery edge includes its later dated attribution source.

Retrieval started with `read_batch.py` packets and the equation-inclusive candidate
inventory from `index_dependency_review_20260919/candidates.json`. Complete packet
and candidate data are retained in `packets-LO-HI.json`; console previews were
bounded. Targeted source passages resolved omitted proof roles, non-heading
ownership, multi-entry route references, imported historical prerequisites and
later scope corrections. This did not reread every entire notebook entry or replay
mathematical computations. The free-column search's non-heading source is hashed
at its explicitly reviewed containing section, `pseudo-live-cell-interface`.

The initially pending route-audit inventory (position 277) was completed after
targeted review of its historical one-block/nested/core references and the failed
batching example. Its mathematical bridge remains open; closing metadata review
does not claim research progress on that bridge.

Key distinctions preserved:

- Prior source audits supply imported Razborov, BIKPRS, Krajíček and BLVZ inputs;
  no fresh paper verification or Lean run was performed.
- Full-space and compact attacks obstruct survival premises, not the logically
  valid conditional implications or Frege itself.
- The later MP audit corrects suggested original theorem-boundary applications,
  retaining conditional algebraic theorems and finite formal-input identities.
- NS-source transfer is not promoted to augmented-PC transfer. Stable NS/PC
  equality is used only for the stated functional-base degree range.
- Method refinements, source examples, imported techniques, finite controls and
  direct proof prerequisites receive different edge types and explicit scope.
- Existing finite certificates were inventoried as evidence, not recomputed or
  represented as new verification. Fixed-target grid necessity remains distinct
  from the old PHP target one.

Reproduce proposal generation and validation under the shared launcher:

```sh
./compute.sh run TURN --threads 1 --category local_processing -- \
  python3 research/results/parallel_index_dependencies_260_360/validate_patch.py
```

Validation checks all 100 claim fingerprints and cited source hashes, distinct
review/edge IDs, schema correctness and local source targets against a temporary
in-memory union with the current registry. It does not import patches. The
build script deliberately regenerates metadata from the current immutable HEAD
and source fingerprints; coordinator integration must recheck these if source
text changes. Graph and affected-endpoint review validation remain coordinator
integration responsibilities.

One preparation run encountered the known non-heading evidence anchor; it failed
closed, then succeeded using the documented containing section. No resource
limits were bypassed. Timing session `parallel_index_dependencies_260_360` is
exported and archived separately; overlapping worker intervals must not be summed
as elapsed wall-clock time. Reading windows include interpretation and drafting
the resulting relationship dispositions; those activities were not separately
instrumented and have not been retrospectively split.
